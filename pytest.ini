[pytest]
DJANGO_SETTINGS_MODULE = ova.settings.dev
python_files = tests.py test_*.py *_tests.py
addopts = --reuse-db
# --reuse-db keeps the test DB between local runs (skip migrations on repeat runs)
# Pass --create-db manually when you add/change a migration to force a rebuild
